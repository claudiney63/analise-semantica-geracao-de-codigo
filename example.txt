var {
    int x, y, a;
}

program {
    scan(x);
    scan(y);
    scan(a);

    if (-x < 0) {
        while (x > -10 and y < 100) {
            print(a);
            y = y + 10;
            x = -x;
        }
    }
}