var {
    int x, y, a;
    float z;
}
program {
    scan(x);
    scan(y);
    scan(a);
    //z = x / 2.0;

    if(a < 4) {
        while(x < 15 and y < 50) {
            print(a);
            y = y * 2;
            x = 20 / x;
        }
    }
}